# --- Core framework ---
Django>=5.0,<5.2
psycopg2-binary>=2.9
python-dotenv>=1.0
whitenoise>=6.5
gunicorn>=21.2

# --- Scraping (Cloudflare bypass) ---
curl_cffi>=0.7
cloudscraper>=1.2.71          # fallback backend
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# --- Async tasks / cache (live engine) ---
celery>=5.4
redis>=5.0
django-celery-beat>=2.6       # admin-editable periodic schedules
django-celery-results>=2.5    # task results visible in admin

# --- Auth (login admin via Google) ---
django-allauth>=65,<66
PyJWT>=2.8
cryptography>=42

# --- Utilities ---
Pillow>=10.0
django-extensions>=3.2
pytesseract>=0.3.10           # OCR de l'axe d'altitude des profils image (échelle)
pywebpush>=2.0               # notifications Web Push (PWA mobile)
